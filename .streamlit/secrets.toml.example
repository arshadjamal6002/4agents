# Paste into Streamlit Cloud → App settings → Secrets (TOML).
# For local Streamlit, copy to secrets.toml (gitignored) if you don't use .env.

OPENAI_API_KEY = "sk-..."
OPENAI_MODEL = "gpt-4o-mini"
LANGFUSE_PUBLIC_KEY = "pk-lf-..."
LANGFUSE_SECRET_KEY = "sk-lf-..."
LANGFUSE_BASE_URL = "https://cloud.langfuse.com"
DEMO_MAX_TOPICS = "2"
